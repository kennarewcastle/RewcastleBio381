# Demonstrate sourcing of a file
# March 8, 2018
# KER

source("MyFunctions.R") # Runs all of the lines of code in this separate file, so loads all of our functions

GetData()
CalculateStuff()
GraphResults()
SummarizeOutput()
